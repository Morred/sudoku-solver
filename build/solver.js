// Generated by CoffeeScript 1.10.0
(function() {
  $(function() {
    var fields, initializeFields, initialized, table;
    fields = [];
    initialized = false;
    table = $(".sudoku table");
    $('.reset').click(function() {
      return console.log('reset grid!');
    });
    $('.next-step').click(function() {
      console.log('next step!');
      if (!initialized) {
        initializeFields();
        initialized = true;
        console.log(fields);
        return console.log("got them!");
      }
    });
    $('.solution').click(function() {
      return console.log('full solution!');
    });
    return initializeFields = function() {
      return table.find('tr').each(function() {
        var row;
        row = $(this);
        fields.push([]);
        row.find('td').each(function() {
          var cell;
          cell = this;
          if (cell.textContent) {
            fields[row.context.rowIndex].push(cell.textContent);
            return $(cell).addClass('fixed-number');
          } else {
            return fields[row.context.rowIndex].push(null);
          }
        });
        return $('td').prop('contenteditable', false);
      });
    };
  });

}).call(this);
